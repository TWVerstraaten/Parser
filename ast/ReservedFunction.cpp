//
// Created by pc on 02-06-21.
//

#include "ReservedFunction.h"

#include <cassert>

gen::Number ast::ReservedFunction::eval(const gen::Number& number) const {
    assert(false);
}

gen::Number ast::ReservedFunction::eval(const gen::Number& number1, const gen::Number& number2) const {
    assert(false);
}
